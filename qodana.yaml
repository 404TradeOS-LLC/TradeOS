version: "1.0"
linter: jetbrains/qodana-js:2026.1

profile:
  name: qodana.starter

bootstrap: |
  set -eu
  cd app && npm ci
  cd ../web && npm ci

exclude:
  - name: All
    paths:
      - packages/knowledge-engine
      - app/dist
      - web/.next
