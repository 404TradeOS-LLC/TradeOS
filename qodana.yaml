version: "1.0"
linter: qodana-js

profile:
  name: qodana.starter

bootstrap: |
  set -eu
  cd app && npm ci
  cd ../web && npm ci

exclude:
  - name: All
    paths:
      - packages/knowledge-engine
      - app/dist
      - web/.next
