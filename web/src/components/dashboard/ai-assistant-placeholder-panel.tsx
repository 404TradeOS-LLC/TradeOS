// Compatibility export for the existing dashboard import. The owner briefing remains
// available as a component for a dedicated drill-in, but the primary owner dashboard
// intentionally renders only the status band, ranked attention queue, and today's work
// before secondary operational context.
export function AIAssistantPlaceholderPanel() {
  return null;
}
